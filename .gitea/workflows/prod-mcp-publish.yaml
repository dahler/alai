#
name: production - mcp - build, publish and deploy Docker image for mcp server on cluster

on:
  push:
    branches: ['master']
    paths:
      - 'mcp_server/**'

jobs:
  build-and-push-image:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Log in to the Container registry
        uses: docker/login-action@v3.6.0
        with:
          registry: ${{ vars.REGISTRY_NAME }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASSWORD }}
      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5.10.0
        with:
          images: ${{ vars.REGISTRY_NAME }}/${{ vars.PROD_MCP_IMAGE_NAME }}
          tags: |
            type=sha
      - name: Build and push Docker image
        id: push
        uses: docker/build-push-action@v6.18.0
        with:
          context: ./mcp_server
          file: ./mcp_server/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
      - name: Update ArgoCD deployment repo
        uses: https://github.com/rikhwanto/actions-modules/update-gitops@v1.2.0
        with:
          gitops_repo: ${{ vars.PROD_GITOPS_REPO }}
          deployment_file: ${{ vars.PROD_MCP_DEPLOYMENT_FILE_NAME }}
          image_tag: ${{ steps.meta.outputs.tags }}
          commit_message: "Alai MCP: ${{ gitea.event.commits[0].message }}"
          gitops_pat: ${{ secrets.ARGOCD_PAT }}
